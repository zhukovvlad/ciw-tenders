# Дизайн: бенчмарк-хранилище gold + оффлайн-метрика качества матчинга

**Дата:** 2026-06-26
**Статус:** черновик на ревью
**Связано:** [2026-06-23-estimate-matching-sp2-design.md](2026-06-23-estimate-matching-sp2-design.md),
[2026-06-25-estimate-org-header-filter-design.md](2026-06-25-estimate-org-header-filter-design.md)
**Следующий под-проект (Спека B):** админ-управление бенчмарками — endpoints (только
admin) + третья вкладка на фронте (список бенчмарков, drill-in, правка ожидаемой
статьи на лету). Зависит от этой спеки; на первую метрику не влияет.

## Декомпозиция

Эта спека — под-проект **A** (хранилище + харнесс). Вместе они дают первую измеримую
пользу (метрику top-1/top-3). Курирующий админ-UI вынесен в **Спеку B** следующим
под-проектом: он зависит от хранилища, но для получения метрики не нужен.

## Проблема

В пайплайне матчинга накоплено много логики: оргфильтр (`status='excluded'`),
чистка крошки от ORG-предков, лексический + LLM-классификатор, порог уверенности
0.90, LLM-арбитр для топ-3. Но **объективной метрики качества нет**. Тюнинг порога,
обогащения крошки и промптов идёт «на глаз»; нельзя ни доказать, что вчерашняя фича
окупилась по точности, ни поймать регрессию (напр. перекос «раздел↔дети»).

Специалист вручную разметил образец сметы до конца: из **809 узлов** **783** получили
эталонную статью СМР, **26** оставлены без метки (**25** оргкаркас этапов/корпусов +
**1** work-домен без статьи — узел `10` «Инженерные системы»). Это ground-truth,
ссылающийся на тот же справочник `template_articles`, что и `Шаблон.xlsx`. Он даёт
инструмент измерения **текущего реализованного пайплайна как есть**.

**Цель v1:** (1) **бенчмарк-хранилище** в БД — gold становится durable приватным
референс-данным (не зависит от локального xlsx); (2) **оффлайн-скрипт**, который
читает gold из БД, прогоняет образец через реальный пайплайн и выдаёт две группы
метрик — качество **классификации** (оргфильтр) и качество **матчинга** (top-1 /
top-3) — плюс пофайловый отчёт расхождений для разбора глазами.

Это фундамент: сам по себе он ничего не улучшает, но превращает все последующие
эксперименты (дедуп крошки, обогащение `embedding_input`, few-shot арбитра, порог) в
измеримый цикл.

## Источник истины: бенчмарк-хранилище в БД

Gold — это **persistent reference data**, родственник `template_articles`: курируемый,
стабильный, не-per-request. Живёт в БД, а не в локальном файле.

**Почему в БД, а не в git-фикстуре:** единственная копия эталона сейчас — xlsx в
`temp/` (gitignore); в git его не закоммитить (структура проекта MR Group), машина
общая. Приватная Neon — уместный durable-дом для таких данных; git — нет.

### Модель данных (новые таблицы)

- `benchmark` — `id`, `name`, `created_at`. Одна размеченная смета = один бенчмарк;
  таблица накапливает несколько смет в будущем.
- `benchmark_node` — `id`, `benchmark_id` (FK→benchmark), `code` (строка раздела,
  напр. `6.3.1`), `name`, `expected_kind` (**ENUM, NOT NULL**),
  `expected_article_code` (**nullable**),
  `expected_article_name` (**nullable**, снимок имени статьи на момент разметки).
  **Все** узлы сметы (вкл. структурные) — иначе дерево/крошка/классификация при
  прогоне будут неполными.
  - **`expected_kind ∈ {matchable, structural, no_article}`** — явный ожидаемый вид
    узла, не выводимая бинарность «код задан/пуст» (см. «Три вида узла» ниже).
    `expected_article_code`/`_name` non-null **только** при `kind='matchable'`.
  - `expected_article_code` **без FK** на `template_articles`: справочник правят
    другие департаменты, gold должен пережить правку каталога; рассинхрон (метка
    ссылается на удалённую статью) — это сигнал (корзина `gold_not_in_catalog`), а не
    ошибка целостности.
  - `expected_article_name` — **снимок текста статьи** на момент разметки. Нужен для
    двух целей: (1) **детектор тихого дрейфа** — статью переименовали под тем же кодом
    (харнесс сравнивает снимок с текущим именем статьи по этому коду → флаг
    «article_renamed»); (2) бенчмарк **самодостаточно читаем** без джойна к справочнику
    (пригодится вкладке Спеки B).
- Миграция — ручная/автоген Alembic + ORM-модели синхронно (как требует CLAUDE.md).

### Сид из xlsx (разовая админ-операция, CLI)

- Скрипт сидинга: `temp/Смета - образец размеченная до конца.xlsx` → строки `benchmark`
  + `benchmark_node`. Путь — параметр (`--gold <path>`). xlsx нужен **только здесь**;
  в рантайме харнесс читает из БД.
- **Извлечение метки узла:** для строк-узлов (есть `№ раздела`, код целочисленно-
  сегментный) берём колонку «Статья СМР». Код статьи нормализуем тем же правилом, что
  и [template_parser.py:51](../../../backend/app/services/template_parser.py#L51):
  `^\((.*?)\)` → `group(1)` → убрать пробелы → `strip(".")`. Итог: `(6.3.1) Name` → `6.3.1`.
  Текст после кода (`group(2)`, нормализованный тем же `norm_name`) → `expected_article_name`.
- **Проставление `expected_kind` — гейт ПОДСКАЗЫВАЕТ, человек РЕШАЕТ.** «Существует ли
  для этой работы статья» — знание о каталоге, из имени невыводимое; автоматом класс не
  поставить. Схема сида:
  - «Статья СМР» задана → `matchable` (автоматически, + код и снимок имени);
  - ячейка пуста → запустить **гейт работной головы** (тот же сигнал, что в оргфильтре:
    `contains_org_token` и/или `not has_work_word` из
    [classification.py](../../../backend/app/domain/classification.py)). Пусто +
    (оргтокен ИЛИ нет работной головы) → подсказка `structural`; пусто + нет оргтокена +
    есть работная голова → подсказка `no_article`;
  - сид **громко печатает** все узлы-подсказки `no_article` (на этом gold их ровно 1 —
    узел `10` «Инженерные системы») и **требует подтверждения человеком** — финальный
    `kind` ставит специалист, не гейт.
  Гейт здесь — подсказчик-валидатор при засидке, а не решатель.
- **Запись в v1 — только CLI-сид.** Правка `kind`/меток через admin-API/UI — Спека B;
  в этой спеке бенчмарк иммутабелен после засидки (пере-сид = пересоздать бенчмарк).

### Три вида узла (`expected_kind`)

Не бинарность «код задан/пуст», а три различных по **ожидаемому поведению пайплайна**
вида:

- **`matchable`** — есть `expected_article_code`; ждём top-1/top-3 на эту статью.
- **`structural`** — оргкаркас (этап/корпус); правильное поведение = **исключить** (в
  корзину).
- **`no_article`** — work-домен, для которого в справочнике статьи нет (статьи у
  детей), напр. узел `10` «Инженерные системы»: оргтокена нет, работная голова есть.
  Правильное поведение = **оставить видимым, но НЕ сматчить** (`no_match`). Оргфильтр
  здесь ведёт себя верно (оргтокена нет → WORK → остаётся); в классификаторе менять
  нечего — отдельным делает его именно ожидаемый результат матчинга.

Принципиальная разница: `structural` пайплайн **должен исключить**, `no_article` —
**оставить и не сматчить**. Смешивать их в одну «пустую» ячейку нельзя — иначе матрица
Группы A зачтёт `no_article` как ложный FP оргфильтра (фантом при разборе).

- **Гранулярность — узлы** (`EstimateNode`): меряем ровно то, что размечал специалист
  и что матчит пайплайн. Листовые позиции — вне области.
- **Дубли кодов** (один код раздела в разных этапах, напр. `3.1.4.1` дважды)
  сравниваются **построчно** (per node), а не схлопываются.

## Что считаем «правильным»

Сравнение по **коду статьи** (стабильный ключ), не по имени (у имён косметические
расхождения регистра/точки). Две независимые группы метрик:

### Группа A — классификация (валидация оргфильтра)

Матрица считается **только по `structural` и `matchable`** (`no_article` исключён —
см. ниже):

| | пайплайн исключил | пайплайн оставил |
|---|---|---|
| **`structural`** (оргкаркас) | TN — верный отброс | FP — оставил мусор (дёшево: оператор отбракует) |
| **`matchable`** (работа) | **FN — молчаливый пропуск работы (дорого!)** | TP — верно оставил |

Ключевая цифра — **FN** (исключили реальную работу): это самая дорогая ошибка по
асимметрии из оргфильтр-спеки. Идеал: FN = 0, FP мал.

### Группа A′ — мини-метрика `no_article`

`no_article` из матрицы A **исключён полностью** (иначе зашумляет FP фантомом).
Отдельная мини-метрика: из `no_article`-узлов какая доля корректно ушла в `no_match`,
а какая получила **уверенный (ошибочный) матч**. Уверенный матч на `no_article` — это
**precision-ошибка**, её надо видеть отдельно, не смешивая с FP оргфильтра. См. также
«Асимметрия: no_article» ниже — рост доли `no_article` сам по себе подозрителен.

### Группа B — матчинг (только по `matchable` И пайплайн-оставил)

`no_article` в Группу B не идёт — gold-кода у него нет.

### Асимметрия: `no_article` может ПРЯТАТЬ промах

`no_article` — потенциальное место, где тихо прячется настоящий промах (родственник
FN): если статью пометили «нет», а она в каталоге **есть** и пайплайн её просто не
нашёл, мы спишем реальный промах в «легитимный пробел». Поэтому:

- состояние `no_article` должно быть **осознанным и аудируемым**: на бенчмарке —
  подтверждено человеком при сиде (громкая печать + подтверждение); в проде (Спека B) —
  кто/когда пометил;
- **харнесс считает долю `no_article` и показывает её в сводке.** Рост этой доли —
  сигнал не «много пробелов в каталоге», а «возможно, маскируем промахи».


- **top-1 accuracy:** код статьи, выбранной пайплайном (`NodeMatch.article_code`,
  будь то `confident` или `needs_review`), == gold-код.
- **top-3 (retrieval) accuracy:** gold-код присутствует среди топ-3 кандидатов
  (`NodeMatch.candidates[*].code`). Изолирует качество векторного поиска от арбитра.
- **Разбивки:**
  - по статусу: `confident` (порог >0.90) vs `needs_review` (арбитр) vs `no_match`;
  - по сложности: «лёгкие» (имя узла == статья после нормализации) vs «переводы»
    (имя ≠ статья) — на финальном gold это **345 vs 438** (сумма 783): переводов больше
    половины, потолок «лёгких» меньше, реальный сигнал качества живёт в переводах;
  - bucket **`gold_not_in_catalog`**: gold-код отсутствует в текущем справочнике (специалист
    сослался на статью, которой в `Шаблон.xlsx` уже/ещё нет) — такие узлы top-1/top-3
    в принципе не возьмут; выносим в отдельную корзину, чтобы не занижать accuracy.
  - флаг **`article_renamed`**: gold-код в справочнике есть, но текущее имя статьи ≠
    `expected_article_name` (снимок). Метка под тем же кодом семантически уехала —
    помечаем в отчёте, чтобы поймать **тихий дрейф** справочника. **К обоим именам
    (снимок и текущее) применяется одно и то же `norm_name`** — иначе флаг будет
    фолсить на регистре/точке, а не ловить настоящий дрейф.

**Детерминизм — нестабильны ОБЕ LLM-точки.** Стабильны: top-3 retrieval (при
фиксированных эмбеддингах) и **лексические** ячейки классификации. Плавают между
прогонами:
- top-1 на ветке `needs_review` — зависит от **LLM-арбитра**;
- FP/FN узлов-**с оргтокеном** в Группе A — зависят от **LLM-классификатора** на ветке
  смеси (тоже LLM!).

Без этой оговорки дрожь классификатора при чтении сводки примут за регрессию оргфильтра.
Митигация: `temperature=0` на обеих точках, опц. N прогонов с указанием разброса.
В отчёте детерминированные метрики (retrieval, лексика) и LLM-зависимые разделены.

## Подход к прогону: интеграционный (реюз production-пути)

**Выбрано (рекомендация):** прогонять образец через **реальные сервисы против реальной
БД и справочника**, как это делает воркер. Скрипт:

1. Строит реальные адаптеры из `get_settings()` + сессия БД (зеркало
   [deps.py `build_estimate_matching_service`](../../../backend/app/api/deps.py#L175)):
   `OpenRouterEmbedder`, `OpenRouterLLMMatcher`/`Anthropic`, `OpenRouterWorkClassifier`,
   `SqlAlchemyArticleRepository`, `SqlAlchemyEstimateRepository`, `MatchingService`,
   `EstimateMatchingService`.
2. Читает `benchmark_node` из БД и реконструирует **транзиентную смету** напрямую
   через `EstimateRepository` (`code`+`name` → узлы; `parent_code`/глубина выводятся
   из кода), минуя xlsx, S3 и Celery. `embedding_input` пайплайн пересоберёт сам на
   шаге классификации. Сметы в системе и так транзиентны.
   - **Инвариант (критично):** реконструкция ОБЯЗАНА воспроизводить тот же `code` и ту
     же сегментную иерархию предков, что и продовый `EstimateParser` (предки/крошка
     строятся по сегментам кода — см. org-filter-план). Иначе крошка на прогоне
     разойдётся с продовой, и харнесс будет мерить **не тот текст, что эмбеддится в
     проде** — ровно тот «дрейф достоверности», ради которого отвергнут lean-харнесс.
     Закрыть **тестом**: одинаковый код узлов и одинаковый `embedding_input` на
     реконструкции против парсера на одном входе.
3. Вызывает `EstimateMatchingService.match_estimate(estimate_id)` — тот самый
   production-метод (classify → embed → gate → match → status).
4. Читает узлы обратно через репозиторий (статус, выбранная статья, снимок кандидатов),
   сопоставляет с gold, считает метрики A и B.
5. **Удаляет транзиентную смету** (cleanup в `finally`).

*Плюс:* максимальная достоверность — мерим точно тот код, что работает в проде
(включая classify/gate/breadcrumb/арбитра). Дублирования логики в харнессе нет.
*Минус:* пишет транзиентную смету в БД (удаляем после) и делает реальные API-вызовы
(эмбеддинг ~760 узлов + арбитр на ветке `needs_review`). Стоимость разовая и умеренная.

**Отвергнуто — «lean-харнесс»** (переписать оркестрацию в скрипте, вызывая доменные
функции + порты напрямую): меньше мутаций БД, но **дублирует** classify+breadcrumb-логику
из `EstimateMatchingService` → риск дрейфа, харнесс начнёт мерить «почти прод», а не прод.
Достоверность важнее экономии записи в БД.

## Предусловия запуска

- Справочник загружен и **проэмбежен** (`just embed-worker`): харнесс зависит от
  `search_similar` по pgvector. Фейл-фаст: перед прогоном проверить
  `ArticleRepository.matching_readiness()` (`total>0 и pending==0`) и понятно упасть,
  если справочник не готов — иначе gate в `match_estimate` поднимет `DictionaryNotReadyError`.
- Валидный `backend/.env` (ключи OpenRouter/Anthropic, `DATABASE_URL`).

## Размещение и запуск

- **Миграция:** Alembic-ревизия с `benchmark` / `benchmark_node` + ORM-модели синхронно.
- **Сид-скрипт:** `backend/app/scripts/benchmark_seed.py`. Рецепт
  `just benchmark-seed gold="<path>"` → `uv run python -m app.scripts.benchmark_seed --gold ...`.
  Читает xlsx, создаёт бенчмарк + узлы. Разовая админ-операция (CLI, как `create-admin`).
- **Харнесс:** `backend/app/scripts/eval_matching.py` (рядом с `create_admin`, `smoke_import`).
  Рецепт `just eval-matching [benchmark="<name>"]` →
  `uv run python -m app.scripts.eval_matching ...`. Читает gold **из БД**, не из xlsx.
- Флаги харнесса: `--benchmark <name|id>` (какой бенчмарк прогнать; дефолт — если он
  один), `--report <path>` (CSV-детализация; дефолт — gitignored-папка), `--keep` (не
  удалять транзиентную смету, для отладки).
- **Не pytest:** харнесс ходит в реальную БД/AI и стоит денег — это инструмент-прогон,
  а не юнит-тест. Доменные правила извлечения/нормализации gold-кода покрываем
  отдельными юнит-тестами (см. «Тесты»).

## Вывод

1. **Сводка в stdout:** Группа A (TN/FP/FN/TP, явно выделить FN); Группа A′ (доля
   `no_article` → `no_match` vs ошибочный уверенный матч + **доля `no_article` от всех
   узлов** — индикатор маскировки промахов); Группа B (top-1, top-3 retrieval, разбивки
   по статусу/сложности, размер `gold_not_in_catalog`, число `article_renamed`).
   Детерминированные (retrieval, лексика) и LLM-зависимые цифры разделены.
2. **CSV-детализация** (в `--report`, по умолчанию в gitignored-папку): по строке на
   узел — `code, name, expected_kind, gold_code, gold_name, kept(bool), status,
   chosen_code, top3_codes, top1_hit, top3_hit, bucket, article_renamed(bool)`. Рабочий
   материал для разбора расхождений глазами.

## Слои (Clean Architecture)

- **domain:** чистые функции извлечения/нормализации gold-кода (`parse_gold_code`),
  построения матрицы классификации и подсчёта top-1/top-3 — без I/O, тестируемы.
- **infrastructure/db:** `BenchmarkRepository` (создать бенчмарк+узлы при сиде, читать
  узлы при прогоне) + ORM-модели `benchmark`/`benchmark_node`.
- **infrastructure:** чтение xlsx (openpyxl) — адаптер сида.
- **scripts:** `benchmark_seed` (xlsx → БД) и `eval_matching` (БД → транзиентная смета
  → `match_estimate` → чтение результатов → сводка/CSV/cleanup).
- Харнесс реюзает существующие сервисы/порты матчинга — новых портов матчинга не вводим;
  единственная новая абстракция — репозиторий бенчмарков.

## Обработка ошибок

- Справочник не готов / `.env` неполон → понятный фейл-фаст до прогона.
- Падение в середине прогона → транзиентная смета всё равно удаляется (`finally`),
  кроме `--keep`.
- Узлы с транзиентной AI-ошибкой (`status='error'`) пайплайн уже гасит поузлово; в
  отчёте такие узлы попадают в bucket «error» и не засчитываются как промах матчинга.

## Тесты (pytest)

- `parse_gold_code`: `(6.3.1) Name` → `6.3.1`; пустая ячейка → `None`; мусорная
  строка → `None`; trailing-dot/пробелы нормализуются как в `template_parser`.
- **Подсказка `expected_kind` при сиде:** «Статья СМР» задана → `matchable`; пусто +
  оргтокен/нет головы → `structural`; пусто + есть голова → `no_article` (узел
  `10` «Инженерные системы»).
- Матрица классификации: считается только по `structural`+`matchable` → корректные
  TN/FP/FN/TP; `no_article` в матрицу A **не попадает**.
- Мини-метрика A′: `no_article` → `no_match` (ок) vs уверенный матч (precision-ошибка).
- Подсчёт top-1/top-3: на синтетических `NodeMatch` + gold — хиты/промахи, bucket
  `gold_not_in_catalog`, разбивка по статусу.
- `article_renamed`: один и тот же `norm_name` к снимку и текущему имени → косметика
  (регистр/точка) **не** триггерит флаг; реальная смена имени триггерит.
- **Паритет реконструкции (2.1):** реконструкция дерева из `benchmark_node` даёт тот же
  `code` и тот же `embedding_input`, что и `EstimateParser` на исходном входе.
- Сам прогон (сеть/БД) тестом **не покрываем** — это инструмент.

## Вне области (YAGNI / отдельные под-проекты)

- **Спека B — админ-управление бенчмарками:** admin-only endpoints (CRUD над
  `benchmark`/`benchmark_node`, импорт), ролевой гейтинг, **третья вкладка на фронте**
  (список бенчмарков, drill-in, inline-правка ожидаемой статьи/`kind` на лету с
  подсветкой `gold_not_in_catalog`/`article_renamed`). Зависит от этой спеки; на первую
  метрику не влияет.
- **Стык с продом — концепт «статьи нет» для оператора** (→ Спека B / ревью-поток):
  тот же `no_article` нужен оператору, чтобы раздел не висел вечным `needs_review` с
  навязанным кривым матчем. Капабилити **почти готова** — у ревью-сервиса уже есть
  `_reject` (`ReviewStatus.REJECTED`,
  [estimate_review_service.py:87](../../../backend/app/services/estimate_review_service.py#L87)),
  т.е. «нет приемлемой статьи» как осознанное действие. В объём A не входит; здесь —
  чтобы концепт не потерялся.
- **Несколько размеченных смет / агрегат по корпусу смет** — схема `benchmark` это уже
  поддерживает (несколько строк), но харнесс v1 гоняет один бенчмарк за раз.
- **Кэш эмбеддингов** для удешевления повторных прогонов (тексты узлов
  детерминированы) — оптимизация на потом.
- **Метрика на листовых позициях** (протягивание метки узла вниз) — листья наследуют
  метку, ground-truth слабее; добавим, когда понадобится.
- **CI-гейт / порог регрессии** — сначала накопить baseline-числа вручную.
- **Применение результатов** (обогащение `embedding_input`, few-shot арбитра, тюнинг
  порога) — отдельные спеки, которые гоняются *через* эту линейку.
- **Неадъяцентный дедуп крошки** (`MR Base . X . MR Base`) — соседний уже закрыт
  `_collapse_consecutive`; неадъяцентный редок, phase-2.

## Проверки на данных (выполнены 2026-06-26)

Прогон на `temp/Смета - образец размеченная до конца.xlsx` (теми же доменными
функциями `contains_org_token`/`has_work_word`):

- Узлов с кодом: **809**; `matchable` (есть «Статья СМР»): **783**; без метки: **26**.
- Из 26 без метки: **25 `structural`** (чистый каркас — «1 Этап ЖК», «2 Этап БЦ»,
  «1 и 2 Этапы БЦ и ЖК») + **1 `no_article`** (узел `10` «Инженерные системы»: оргтокена
  нет, работная голова есть).
- Разбивка сложности `matchable`: **лёгкие 345 / переводы 438** (сумма 783) — переводов
  больше половины.
- `gold_not_in_catalog`: **0 из 283** уникальных gold-кодов отсутствует в `Шаблон.xlsx`
  (362 кода). Корзина пуста — это заодно подтверждает **паритет нормализации gold-кода
  против `article_code`** (иначе вылезли бы ложные «отсутствующие»). Корзину оставляем —
  на будущих сметах/версиях каталога её ненулёвость станет честным сигналом дрейфа.

## Definition of Done

- Миграция `benchmark`/`benchmark_node` (с `expected_kind ENUM`) применяется
  (`just migrate`); ORM синхронны.
- `just benchmark-seed gold="temp/Смета - образец размеченная до конца.xlsx"` создаёт
  бенчмарк со **всеми 809** узлами; подсказка `expected_kind` по гейту, **громкая печать
  + подтверждение человеком** для `no_article` (на этом gold — 1 узел); `matchable`
  сохраняет код и снимок имени статьи.
- Харнесс помечает **дрейф справочника**: `gold_not_in_catalog` (код исчез) и
  `article_renamed` (имя под кодом изменилось vs снимок, сравнение через единый `norm_name`).
- `just eval-matching` читает gold **из БД**, реконструирует транзиентную смету
  (инвариант: тот же `code`/`embedding_input`, что и `EstimateParser`), прогоняет
  реальный `match_estimate` и печатает сводку.
- Группа A (только `structural`+`matchable`) считает TN/FP/FN/TP; **FN выделен явно**.
- Группа A′: `no_article` отдельно (доля → `no_match` vs ошибочный матч + доля от всех).
- Группа B (только `matchable`) считает top-1 и top-3 retrieval с разбивками по статусу
  и сложности (345/438) и корзиной `gold_not_in_catalog`.
- Сводка разделяет детерминированные (retrieval, лексика) и LLM-зависимые (арбитр +
  классификатор смеси) метрики.
- CSV-детализация по узлам пишется в gitignored-путь; данные сметы в репозиторий не
  коммитятся.
- Транзиентная смета удаляется после прогона (кроме `--keep`).
- Фейл-фаст, если справочник не проэмбежен.
- Доменные функции (parse/нормализация/подсчёт/паритет реконструкции) покрыты
  юнит-тестами; сетевой прогон — нет.
